import React, { useEffect } from 'react';

const Site360Detail = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return (
        <div className="min-h-screen bg-[#00001a]">
            {/* Page content cleared for manual redesign */}
        </div>
    );
};

export default Site360Detail;
